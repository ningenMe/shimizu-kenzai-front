export interface CompanyContent {
    title: string,
    list: ReadonlyArray<string> 
}

export interface WorkImageContent {
    title: string,
    body: string,
    imageTitle: string,
    src: string
}